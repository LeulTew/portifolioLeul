/** The id a record's article carries, so navigation can land the linear page on it. */
export const educationRecordId = (index: number) => `education-record-${index}`;

/** The rail's own id: where the staged reader is taken back to when a layout change resumes it. */
export const EDUCATION_RAIL_ID = 'education-rail';
