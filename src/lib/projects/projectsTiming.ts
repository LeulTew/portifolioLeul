/**
 * The TV chapter's clock and the window it stages in, apart from the TV's
 * geometry: the DOM reader times its playback by them, and must not pull
 * three into the no-WebGL page (round 10, TECH-029).
 */
export const PROJECTS_TURN_MS = 2200;
export const PROJECTS_APPROACH_MS = 1400;
export const PROJECTS_STAGE_QUERY = '(min-width: 900px) and (min-height: 560px)';
